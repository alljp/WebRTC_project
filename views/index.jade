extends layout

block content
  script(type='text/javascript').
    var room = "#{ params.room }";
    var initCall = #{ room_count > 0 };
    console.log(initCall);

  video#vid1(width='100%', height='100%', autoplay)
  video#vid2(width='100%', height='100%', autoplay)

